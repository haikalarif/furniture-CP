{
  "phases": {
    "setup": {
      "nixPkgs": [
        "php82",
        "php82Packages.composer"
      ]
    },
    "install": {
      "cmds": [
        "composer install --no-dev --optimize-autoloader --no-interaction"
      ]
    },
    "build": {
      "cmds": [
        "chmod +x start.sh"
      ]
    }
  },
  "start": {
    "cmd": "./start.sh"
  }
}
